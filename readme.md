add rules yourself if you want
import { specifiedRules } from "graphql/validation/specifiedRules";

have to provide validate
and validate schema if you want to

pass in context as second arg to event
